
# git仓库配置
spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true # 从配置中心读取文件
        service-id: config-server # 配置中心的serviceId，即服务名
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/ #注册地址
